name: Test

on:
  push:
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - run: Add-Content $env:GITHUB_PATH $env:KIT_PATH
      - run: echo $env:GITHUB_PATH
      - run: ls "C:\Program Files (x86)\Windows Kits\10\App Certification Kit"
      - run: makeappx.exe

